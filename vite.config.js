import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // For Vercel deployment, no base path is needed
  // base: '/anyasgift/', // Only needed for GitHub Pages
});